name: Build and test
run-name: ${{github.actor}} is running build-test workflow 🚀

on: push

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "build application"

  Test:
    needs: Build
    runs-on: ubuntu-latest
    steps:
      - run: echo "run tests"
